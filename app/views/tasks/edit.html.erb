<%= render(partial: 'back') %>

<div class="tasks new">
  <h2>Edit Task</h2>

  <%= form_for(@task) do |f| %>
    <% # url: tasks_path(@task), method: 'patch' (This is just a comment)%>

    <%= render(partial: 'form', locals: {f: f}) %>

    <div class="form-buttons">
      <%= f.submit("Edit Task") %>
    </div>

  <% end %>
</div>